import java.util.Random;

public class CreditCardService {
    public boolean chargeCreditCard(CreditCard card, int money) {
        // assume that this service is very complex,
        // and testing over it may be difficult due to financial issues.
        return new Random().nextBoolean();
    }
}
